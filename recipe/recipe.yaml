context:
  name: csp-bot
  version: "2.0.1"
  python_min: "3.11"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/c/csp-bot/csp_bot-${{ version }}.tar.gz
  sha256: b864c732454c1c0d57e5c2d4dc6a1b2fa5cff934db4f19f2b363c51152f4aa83

build:
  number: 1
  noarch: python
  script: python -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - hatchling
  run:
    - python >=${{ python_min }}
    - beautifulsoup4
    - ccflow >=0.5.2
    - chatom >=0.2.0
    - croniter
    - csp >=0.9,<1
    - csp-gateway >=2.5,<3
    - csp-adapter-discord
    - csp-adapter-slack
    - csp-adapter-symphony
    - dateparser
    - hydra-core
    - pandas
    - pydantic >=2

tests:
  - python:
      imports:
        - csp_bot
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - "*"

about:
  license: Apache-2.0
  license_file: LICENSE
  summary: Reactive chat bot framework
  homepage: https://github.com/Point72/csp-bot/wiki
  repository: https://github.com/Point72/csp-bot

extra:
  recipe-maintainers:
    - arhamchopra
    - ptomecek
    - timkpaine
